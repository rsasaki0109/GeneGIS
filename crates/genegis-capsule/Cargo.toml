[package]
name = "genegis-capsule"
description = "Open, content-addressed GeneGIS result capsules and offline verification"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
base64 = "0.22"
ed25519-dalek = "2"
genegis-analysis = { path = "../genegis-analysis" }
genegis-contract = { path = "../genegis-contract" }
genegis-core = { path = "../genegis-core" }
genegis-workflow = { path = "../genegis-workflow" }
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true
uuid.workspace = true
thiserror.workspace = true

[dev-dependencies]
tempfile = "3"

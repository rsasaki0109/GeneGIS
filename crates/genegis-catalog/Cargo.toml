[package]
name = "genegis-catalog"
description = "GeneGIS catalog engine — dataset metadata registry"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
genegis-core = { path = "../genegis-core" }
genegis-geometry = { path = "../genegis-geometry" }
genegis-raster = { path = "../genegis-raster" }
genegis-storage = { path = "../genegis-storage" }
genegis-workflow = { path = "../genegis-workflow" }
chrono.workspace = true
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true

[dev-dependencies]
genegis-vector = { path = "../genegis-vector" }
